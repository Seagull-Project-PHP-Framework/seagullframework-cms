ALTER TABLE `page`
  ADD FOREIGN KEY (`parent_id`) REFERENCES `page` (`page_id`)
  ON DELETE CASCADE ON UPDATE CASCADE;

ALTER TABLE `page`
  ADD FOREIGN KEY (`site_id`) REFERENCES `site` (`site_id`)
  ON DELETE RESTRICT ON UPDATE CASCADE;

ALTER TABLE `page`
  ADD FOREIGN KEY (`created_by`) REFERENCES `usr` (`usr_id`)
  ON DELETE RESTRICT ON UPDATE CASCADE;

ALTER TABLE `page`
  ADD FOREIGN KEY (`updated_by`) REFERENCES `usr` (`usr_id`)
  ON DELETE RESTRICT ON UPDATE CASCADE;

ALTER TABLE `page_trans`
  ADD FOREIGN KEY (`page_id`) REFERENCES `page` (`page_id`)
  ON DELETE CASCADE ON UPDATE CASCADE;

ALTER TABLE `category2`
  ADD FOREIGN KEY (`parent_id`) REFERENCES `category2` (`category2_id`)
  ON DELETE CASCADE ON UPDATE CASCADE;

ALTER TABLE `route`
  ADD FOREIGN KEY (`page_id`) REFERENCES `page` (`page_id`)
  ON DELETE CASCADE ON UPDATE CASCADE;

ALTER TABLE `route`
  ADD FOREIGN KEY (`site_id`) REFERENCES `site` (`site_id`)
  ON DELETE RESTRICT ON UPDATE CASCADE;

ALTER TABLE `site`
  ADD FOREIGN KEY (`site_id`) REFERENCES `site` (`site_id`)
  ON DELETE RESTRICT ON UPDATE CASCADE;
